web: bash -lc 'set -e; mkdir -p models; if command -v python >/dev/null 2>&1; then P=python; else P=python3; fi; $P -c "import os,urllib.request,os.path; u=os.environ.get(\"MODEL_URL\"); d=\"models/consumption_prediction_xgb.pkl\"; (u and not os.path.exists(d)) and urllib.request.urlretrieve(u,d)"; exec uvicorn app.main:app --host 0.0.0.0 --port $PORT'
